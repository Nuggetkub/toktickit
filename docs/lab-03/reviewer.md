# Lab 3 Peer Review Record

**Author:** Sittijed Jantarataeme — Student ID 67070501046 — GitHub [@Nuggetkub](https://github.com/Nuggetkub)
**Peer reviewer:** Pattharapon Kijjanukij — Student ID 67070501069 — GitHub [@Earth2509](https://github.com/Earth2509)

Both students review each other's work. This record is written from actual GitHub activity
only — every row corresponds to a real review, comment, or merge that can be opened at the
linked URL. No approval is recorded before it happens.

## Review Workflow

**This section describes `Nuggetkub/toktickit`, the repository this document lives in.**
Each Lab 3 issue, [#44](https://github.com/Nuggetkub/toktickit/issues/44) to
[#57](https://github.com/Nuggetkub/toktickit/issues/57), is implemented on its own feature
branch and enters `lab3-staging` through a peer-reviewed Pull Request. The release Pull
Request from `lab3-staging` to `main` requires review and approval as well. Both branches
are protected: one approving review is required, stale approvals are dismissed on a new
push, and the rule is enforced for the repository owner too, so an unreviewed merge is not
possible here.

**The two repositories number things independently, and the ranges overlap.** In this
repository the Lab 3 issues are #44–#57 and its Lab 3 pull requests begin at #58; in
`Earth2509/toktickit` the Lab 3 contract is issue #34 and pull request #35. Every reference
below is written as either `Issue #n`, `PR #n` for this repository, or `Earth2509 PR #n`,
and every link resolves to the thing its label names.

---

## Reviews Received on My Pull Requests — `Nuggetkub/toktickit`

| My issue / My pull request | Scope | Reviewer feedback | Author response | Outcome |
|---|---|---|---|---|
| Issue [#44](https://github.com/Nuggetkub/toktickit/issues/44) / [PR #58](https://github.com/Nuggetkub/toktickit/pull/58) | The Sprint 3 engineering contract: `specification.md`, `api-spec.md`, `ui-spec.md`, `tests.md`, `reviewer.md`, `ai-use.md` | **Changes requested**, 2026-09-13, on commit `8cc8431`. Four consistency and coverage gaps: the Change Password checklist in `ui-spec.md` showed only three of the five rules BR-11 states, so a whitespace-only password or one equal to the account email could look valid and still be refused by the API; no planned test proved how a migrated Lab 2 Requester receives an initial password and is forced to change it, which labsheet §5.2 requires to be documented *and* tested; RESP-01 and the screenshot paths omitted My Tickets and Requester Ticket Detail although both change in Lab 3; and §8 said `403` was "for role refusals only" while `api-spec.md` also defines `PASSWORD_CHANGE_REQUIRED`, `ACCOUNT_INACTIVE` and `ORIGIN_REJECTED`. | All four accepted — none needed argument. The checklist now lists all five rules and UI-02 asserts the two that were missing; **DB-05** covers the migrated Requester's initial-password journey and is traced to AC-02 and AC-11; RESP-01 and the screenshot paths now include both Requester screens; §8 and decision D-07 now name all four `403` codes and say why none of them discloses a resource. Traceability re-checked by script. | **Approved** and merged into `lab3-staging` as `3058640` after he re-checked each of the four |
| Issue [#45](https://github.com/Nuggetkub/toktickit/issues/45) / [PR #59](https://github.com/Nuggetkub/toktickit/pull/59) | The `Requester`→`User` migration, scrypt hashing and the fixture seed | **Changes requested**, 2026-09-15. A real defect, and the most useful review I received: `USERS` carried a per-fixture `mustChangePassword` flag which the seed copied when filling a migrated account's empty password, so a migrated Lab 2 Requester received a working password *and* a cleared gate — contradicting BR-12 and DB-05. My own test agreed with the bug, because it compared the database against the same wrong fixture flags. | Accepted without argument. The flag is gone from the fixture type entirely and the seed always sets `mustChangePassword: true` when it issues a password; the migrated test now clears the flag itself before re-seeding. Proved by reverting the fill-empty path — exactly one test failed, "expected false to be true". The general lesson: when a rule can be expressed in two places, one of them eventually disagrees, so the second was deleted rather than corrected. | **Approved** and merged as `7c9f449` |
| Issue [#46](https://github.com/Nuggetkub/toktickit/issues/46) / [PR #60](https://github.com/Nuggetkub/toktickit/pull/60) | The session authentication API: login, current user, change password, logout, and the login throttle | **Changes requested**, 2026-09-15, and again correct. `changePassword` called `recordFailure` on a wrong current password but never read `throttleState` before validating, so it counted attempts that nothing enforced — a stolen session could guess the current password indefinitely. My own commit message had claimed that path was closed; it was not. | Fixed in `18910af` by reading the window first, exactly as sign-in does, plus a test that sends five wrong current passwords from inside a valid session and proves the sixth is refused *although its current password is correct*. Removing the check again fails it: "expected 200 to be 429". | **Approved** and merged as `813ac0f` |
| Issue [#47](https://github.com/Nuggetkub/toktickit/issues/47) / [PR #61](https://github.com/Nuggetkub/toktickit/pull/61) | Role authorization, the session replacing `X-Dev-Requester-Id`, and the authenticated Lab 2 regression | **Changes requested**, 2026-09-16 — not a defect. He called the authorization work well structured but refused to leave `lab3-staging` holding a server that retires `GET /api/requesters`, requires authentication for reference data and drops the identity header, while the client still depended on all three. I had disclosed that gap in the description; his point was that disclosing it is not the same as not leaving it there. | Accepted. I took his first option and coordinated the merge order rather than growing this PR: issue #48 was built on this branch as [PR #62](https://github.com/Nuggetkub/toktickit/pull/62), and this PR stayed open until that one was approved. Stated on both PRs before either merged. | **Approved**, 2026-09-16, and merged as `658e61b` — which carried both changes into staging together, so it never held the server change without a client that could sign in |
| Issue [#48](https://github.com/Nuggetkub/toktickit/issues/48) / [PR #62](https://github.com/Nuggetkub/toktickit/pull/62) | Login, Change Password, Forbidden, the authenticated shell, and the retirement of the Development Requester selector | **Approved**, 2026-09-16, with no changes requested. | No changes were needed. The PR recorded two defects my own new tests had caught before review: `/change-password` decided while `GET /api/auth/me` was still in flight, so reloading that URL bounced the user to Login; and mandatory mode rendered a second Log out beside the shell's. | **Approved** and merged as `d121a24` |
| Issue [#49](https://github.com/Nuggetkub/toktickit/issues/49) / [PR #63](https://github.com/Nuggetkub/toktickit/pull/63) | The IT Staff queue API: `GET /api/staff/tickets` and `GET /api/staff/assignees`, the filter set combining with AND, enum-order sorting and the Ticket Number tie-breaker | [**Approved**, 2026-09-16](https://github.com/Nuggetkub/toktickit/pull/63#pullrequestreview-5225172590), with no changes requested. | No changes were needed. The PR recorded a gap I had closed on myself before review: `api-spec.md` §5 promises the Ticket Number tie-breaker and nothing asserted it, so three tickets now share one timestamp to force the ordering to be decided by the tie-breaker rather than by insertion order. The validator also computes every parameter before the error check, deliberately, because that is the defect I had blocked in his PR #46 — so the unit test exercises each filter on its own, since a combined query is exactly what hid it in his code. | **Approved** and merged as `054451f` |
| Issue [#50](https://github.com/Nuggetkub/toktickit/issues/50) / [PR #64](https://github.com/Nuggetkub/toktickit/pull/64) | The IT Staff Ticket Queue screen: the toolbar, seven columns, both priority badges, the resolution marker, and the mobile cards inherited from `.zen-table` | [**Changes requested**, 2026-09-17](https://github.com/Nuggetkub/toktickit/pull/64#pullrequestreview-5231091160), with four findings — `sortBy=ticketDate` rejected, `owner` and `requesterIndicated` unsupported, `fullName` versus `displayName`, and no requester-name search. **All four describe his queue, not ours.** I verified every one against our merged code before replying: `STAFF_SORT_FIELDS` includes `ticketDate`, the validator's known set contains both parameters, the row select uses `fullName`, and `queueWhere` searches `requester.fullName`. | Replied with file-and-line evidence, and said plainly that I had made the same mistake in his repository twice — `owner=me` and `CLOSED -> REOPENED` — so it read as a shared hazard rather than a rebuke. **His underlying point was right, so I closed it anyway**: our client suites mock `fetch` while our server suites drive the API, so the two halves of one contract were tested independently and could drift silently. Added a "the client's contract" block to `staff-queue.api.test.ts` asserting only the names — the first-load request, every `sortBy` the Sort control emits, every toolbar parameter and the row fields the screen renders. Server 303 tests, was 288. | [**Approved**, 2026-09-17](https://github.com/Nuggetkub/toktickit/pull/64#pullrequestreview-5237191450) and merged as `eba5244` |
| Issue [#51](https://github.com/Nuggetkub/toktickit/issues/51) / [PR #65](https://github.com/Nuggetkub/toktickit/pull/65) | The ticket workflow API: claim, owner, IT Priority and status, each a read-decide-write in one transaction with a version-conditional `updateMany` | [**Approved**, 2026-09-17](https://github.com/Nuggetkub/toktickit/pull/65#pullrequestreview-5237686969), with no changes requested. He named the policy-and-transport separation, the version-conditional transaction, and the independent transition-matrix test alongside real PostgreSQL coverage. | No changes were needed. The PR raised a contract oddity for his view rather than deciding it alone: `api-spec.md` §1 orders validation before state, so resolving a **closed** ticket with no summary answers `400` about the summary rather than `409 TICKET_TERMINAL`. That is pinned deliberately in the unit test. A second gap closed on myself: nothing anywhere asserted that a new ticket's IT Priority starts equal to its Requested Priority, which API-19 promises — the behaviour was already correct and simply had no test. | **Approved** and merged as `a746b44` |
| Issue [#52](https://github.com/Nuggetkub/toktickit/issues/52) / [PR #66](https://github.com/Nuggetkub/toktickit/pull/66) | Public Comments, Internal Notes and the Requester's resolution indication, plus the BR-27 attachment freeze that had never been implemented | **Changes requested twice, and correct both times.** Round one, 2026-09-17 on `8922e93`: a real time-of-check/time-of-use race. `POST /comments` and `POST /internal-notes` read the Ticket, checked `isTerminal`, then inserted with nothing holding the row; and `indicateResolved`'s conditional write keyed only on `{ id, requesterResolvedAt: null }` — no status in the predicate and the count ignored — so a concurrent transition could store an indication on a status BR-32 forbids and still answer `200`. Round two, on `e51a223`: **the same shape was still open in `removeAttachment`**, which read `currentStatus` through the attachment's nested `ticket` select and mutated afterwards without ever locking the Ticket row. | Both accepted without argument. Round one fixed at `8456517`: all three mutations take the Ticket row lock inside their transaction, decide under it and write in the same transaction; api-spec §1's order is untouched, because the lock changes *when* the status is read, not which answer wins. **My first attempt at his requested coverage was worthless and the break-the-code run caught it** — supertest is lazy, so `const pending = fire()` dispatched nothing and the request ran after the helper released its lock, passing identically with and without the lock; fixed at `e51a223` by attaching `.then()`. Round two fixed the same way for `removeAttachment`, with the attachment looked up *before* the status is judged so a missing file is still `404` on a closed Ticket. | [**Approved**, 2026-09-18](https://github.com/Nuggetkub/toktickit/pull/66#pullrequestreview-5243920143) and merged as `7086a1e`. The lesson recorded twice over: **when a race is fixed in one route, grep every sibling route for the same shape before committing** — I applied the lock to uploads and to three discussion routes and still left the fourth, which is precisely how he found it a second time |
| Issue [#53](https://github.com/Nuggetkub/toktickit/issues/53) / [PR #67](https://github.com/Nuggetkub/toktickit/pull/67) | The IT Staff Ticket Detail screen at `/queue/:ticketId`, the shared `ConfirmDialog` and `DiscussionPanel`, and the Requester discussion changes | [**Approved**, 2026-09-18](https://github.com/Nuggetkub/toktickit/pull/67#pullrequestreview-5245983934), with no changes requested. | No changes were needed. **He did catch a process error worth recording: GitHub does not auto-retarget a stacked pull request when its base *merges* — only when the base branch is deleted.** This PR was built on `feature/52-comments-notes-api`, and after #66 merged it still pointed at a branch that no longer existed as a merge target, so it was retargeted by hand and re-run on the merge result. The PR also recorded a gap closed on myself: nothing asserted that the comment thread refreshes after a status change, although BR-30 posts the summary as a comment in the same transaction. | **Approved** and merged as `c2983bb` |
| Issue [#54](https://github.com/Nuggetkub/toktickit/issues/54) / [PR #68](https://github.com/Nuggetkub/toktickit/pull/68) | The Administrator user management API: list, search, role filter, create, edit, activation, initial-password reset, and BR-42's row lock | **Approved on 2026-09-18 at 08:59, then [dismissed automatically](https://github.com/Nuggetkub/toktickit/pull/68#pullrequestreview-5246041491) when I pushed.** Our branch protection dismisses a stale approval on any push, and this PR needed one after #66 and #67 landed in staging ahead of it. | The conflicts were **additive in all three files**, so both sides were kept: `errors.ts` took `INDICATION_NOT_ALLOWED` plus the five admin codes, `app.ts` took both sets of imports and routes, and in `tests.md` **staging's corrected UNIT-06 won over my branch's stale copy**, which still pointed at the wrong file. I commented explaining each resolution rather than leaving him to infer it from the diff, re-ran on the merge result — client 170, server 569 — and re-requested review. | [**Approved again**, 2026-09-18](https://github.com/Nuggetkub/toktickit/pull/68#pullrequestreview-5246872176) and merged as `8ac5346` |
| Issue [#55](https://github.com/Nuggetkub/toktickit/issues/55) / [PR #69](https://github.com/Nuggetkub/toktickit/pull/69) | The Administrator User Management screen at `/users`: one panel in two modes, a separate initial-password section, and the Administrator's landing screen | [**Approved**, 2026-09-18](https://github.com/Nuggetkub/toktickit/pull/69#pullrequestreview-5248857842), with no changes requested. | No changes were needed. The PR recorded a break-the-code result worth keeping: one of the six proofs **stayed green when broken**, because the duplicate-email message also appears in the panel banner, so a `findByText` scoped to the panel matched either one. The fix was to assert *placement* rather than presence — `toBeInvalid()` with `toHaveAccessibleDescription()` — after which the break reddens exactly one named test. A green break can mean the assertion is too loose, not only that a rule is duplicated. | **Approved** and merged as `6d04ba8` |
| Issue [#56](https://github.com/Nuggetkub/toktickit/issues/56) / [PR #70](https://github.com/Nuggetkub/toktickit/pull/70) | The Lab 3 browser suite, the responsive evidence and the `ui-spec.md` §13 visual inspection, plus the repair of the Lab 2 Playwright journeys | [**Feedback on 2026-09-18**](https://github.com/Nuggetkub/toktickit/pull/70#issuecomment-5733756424), left as a conversation comment rather than a formal review — the same shape as his second round on #66, and the reason this log records comments as well as reviews. Three points: three §13 checklist rows were left unticked with reasons recorded, which does not satisfy a labsheet that requires a completed checklist; mojibake in the `package.json` description; and the final results should be recorded from the merged `main`. | **The first two were right and are fixed in `d087dd6`.** The three rows are closed with evidence rather than ticks: a keyboard focus assertion driving the real dialog, the `:focus-visible` outline pinned in the stylesheet test, eight interface states forced at the network edge, and a capture of the Internal Notes surface. Break-proved six of six with a control on **each** runner. The mojibake was mine — my own `0646a58` turned `U+2014` into `U+0E42 U+20AC U+201D`, a UTF-8 em-dash read through this machine's CP874 codepage — and I had read that line earlier and dismissed it as terminal encoding. His third point belongs to issue #57: `tests.md` §6 fills Final Results from `main` after the release merge, which is the same sequencing he chose on his own #53. | [**Approved**, 2026-09-19](https://github.com/Nuggetkub/toktickit/pull/70#pullrequestreview-5254379829) on `d087dd6`, and merged as `de05180`. e2e lab-03 27 passed, client 187, 33 screenshots, contract checker clean |
| Issue [#71](https://github.com/Nuggetkub/toktickit/issues/71) / [PR #72](https://github.com/Nuggetkub/toktickit/pull/72) | This log itself: eleven missing rows, the stale #66 outcome, and two `ai-use.md` defects | [**Changes requested**, 2026-09-19](https://github.com/Nuggetkub/toktickit/pull/72#issuecomment-5733756424), as a conversation comment rather than a review — the third time, and the reason this table now records comments as well as formal reviews. **The finding was exact and deserved:** the row I had just added for PR #70 still read "Open — re-review requested" although he had approved it hours earlier, so merging this pull request would have made the review record stale *on arrival* — the precise rot the Notes policy exists to prevent, reintroduced by the change meant to repair it. | Accepted without argument. Fixed in `fbf8029`: the #70 row now records the approval, links the approval review and names its merge commit. I then pushed `80c74cf` **before** he re-reviewed, adding the row for his PR #54 which I had approved that morning — partly because his own policy says rows are added as reviews happen, and partly because our protection dismisses an approval on push, so adding it afterwards would have cost him a second look for nothing. | [**Approved**, 2026-09-19](https://github.com/Nuggetkub/toktickit/pull/72#pullrequestreview-5255921855) — "the corrected review record now reflects the PR #70 approval and merge, and the ai-use.md table renders correctly as one complete table" — and merged as `87091dc` |
| Issue [#73](https://github.com/Nuggetkub/toktickit/issues/73) / [PR #74](https://github.com/Nuggetkub/toktickit/pull/74) | The README, which still described Lab 2 across six regions and documented two mechanisms Lab 3 had deleted | **Changes requested twice.** [Round one](https://github.com/Nuggetkub/toktickit/pull/74#pullrequestreview-5255939927), 2026-09-19: five API-table rows said to contradict the registered routes — `…/attachments/:id/remove`, and `/api/staff/tickets/:id/{claim,owner,priority,status}`. **All five are routes in his repository, not ours** (his `app.ts` lines 387, 655, 668, 686, 698, against ours at 168 and 199–202), so applying them would have *introduced* the `404`s the finding warned about. [Round two](https://github.com/Nuggetkub/toktickit/pull/74#pullrequestreview-5259569147), 2026-09-20, and **correct**: the Tests code block fails when pasted, because `cd` leaves the shell outside the repository root before the two root-level Playwright scripts run. | Round one answered with file-and-line evidence from **both** repositories, and with the two times I made the identical mistake in his — `owner=me` on his #46, `CLOSED -> REOPENED` on his #47 — so it read as the shared hazard of two repositories implementing one labsheet. He withdrew it silently in round two. Part of it was still ours, so rather than only rebutting I documented why our staff *queries* sit under `/api/staff` while staff *mutations* hang off `/api/tickets/:id` (`0d7b08b`). Round two was right and **worse than reported** — `cd client` also fails from inside `server/` — and was fixed as a *class* across three blocks in `bec40bd`, every command now root-relative via `npm --prefix`. | [**Approved**, 2026-09-20](https://github.com/Nuggetkub/toktickit/pull/74#pullrequestreview-5260822575) and merged as `fedaa54`. The lesson worth keeping from round two: **a README is executable documentation**, and a block that fails when pasted is the same defect as a §13 checkbox explained rather than filled — which he also caught, on #70 |
| Issue [#57](https://github.com/Nuggetkub/toktickit/issues/57) / [PR #77](https://github.com/Nuggetkub/toktickit/pull/77) | **The Lab 3 release**: `lab3-staging` → `main`, 53 commits across 151 files, integrating work already reviewed issue by issue | [**Approved**, 2026-09-21](https://github.com/Nuggetkub/toktickit/pull/77#pullrequestreview-5268233174), with no changes requested and no comments. | No changes were needed. The pull request introduced **no content of its own** — a release integrates reviewed staging work rather than adding to it, which is the rule that had already moved the README rewrite and the two log rows above into their own pull requests. Verification was run **once**, on the final candidate `ecc7ed5`, deliberately held until staging stopped moving: server 28 files / 569, client 14 files / 187, e2e lab-02 10, e2e lab-03 27, both builds clean, contract checker exit 0. The body also flagged that the server log prints `ECONNREFUSED` several times from the deliberate `reference-failure` mock, so a green run would not be misread — the same courtesy he extended on his #53. | **Approved** and merged as `86c30ca`, putting Lab 3 on `main` for the first time. **The pull request deliberately carried no closing keyword**: `main` is the default branch, so `Closes #57` would have fired on merge and left the post-merge evidence with nothing tracking it — precisely how his `Closes #45` orphaned his final-main evidence. Issue #57 stays open until the evidence-only pull request below merges |

## Reviews I Gave on My Peer's Pull Requests — `Earth2509/toktickit`

| His issue / His pull request | Scope | My feedback | His response | Outcome |
|---|---|---|---|---|
| Earth2509 issue [#34](https://github.com/Earth2509/toktickit/issues/34) / [Earth2509 PR #35](https://github.com/Earth2509/toktickit/pull/35) | His Lab 3 engineering contract: `specification.md`, `api-spec.md`, `ui-spec.md`, `tests.md`, `reviewer.md`, `ai-use.md`, `backlog.md` | [**Changes requested**, 2026-09-11](https://github.com/Earth2509/toktickit/pull/35#pullrequestreview-5181355623), on commit `6392536`. I cloned his branch and checked the contract against his code: the baseline commit, the model and route names the migration relies on, and the AC-to-test matrix, which I checked by script. Three blocking items: the Administrator role filter was dropped although labsheet §8.5 and Part 8 require it; "terminal" and "active-work" were used as rules but never defined, and the uses disagreed; the Requester's resolution indication was never shown to IT Staff, which Part 7 grades. Four non-blocking items — a timing-based account-enumeration gap at sign-in, the proxy-versus-CORS transport being undecided, a `401` for a wrong current password, and seeded credentials kept outside Git — plus four nits. | He fixed all seven in `cf87b96`: an authoritative eight-row status policy table, the role filter, the indication shown to IT Staff, dummy-hash verification for unknown and null-hash accounts, the same-origin proxy chosen explicitly, `422` on the current-password field, and documented `example.test` seed credentials. | [**Approved**, 2026-09-12](https://github.com/Earth2509/toktickit/pull/35#pullrequestreview-5181355623) after re-checking each item against his files and re-running his traceability by script; merged into his `lab3-staging` as `d819957` |
| Earth2509 issue [#36](https://github.com/Earth2509/toktickit/issues/36) / [Earth2509 PR #37](https://github.com/Earth2509/toktickit/pull/37) | His authentication foundation: the `Requester`→`User` migration, scrypt hashing, sessions, CSRF, the same-origin proxy, fixture seed, and the Lab 3 test files — 31 files | **Changes requested**, 2026-09-13, on commit `f99687e`. Found by *running* the branch, not reading it: `GET /api/requesters` was still live — his own `api-spec.md` says it returns `404` — and after the table rename it queried `user.findMany` with no role filter, so an unauthenticated `curl` returned IT Staff and Administrator accounts including `admin@example.test`. With his own five-failure throttle that also allows an anonymous caller to lock out the only Administrator. His existing test mocked `findMany` wholesale and so could never catch it. Three non-blocking notes: the migration suite self-skips without its environment variable; the `tests.md` Final column held prose about a feature-branch run; four nits. | He fixed it in `b564a06`: the route now filters `role: "REQUESTER"`, with a real-Prisma regression assertion, startup configuration validation, `INTERNAL_ERROR` on 500 bodies, and `tests.md` restored to `Planned`/`Passed`. | [**Approved**, 2026-09-13](https://github.com/Earth2509/toktickit/pull/37) and merged as `aeb38a9`. I re-probed the running API — only the four Requester fixtures are returned — and then **removed his role filter to prove his new test fails**: it did, at `migration.test.ts:87`, exit code 1, and passed again once restored |
| Earth2509 issue #38 / [Earth2509 PR #39](https://github.com/Earth2509/toktickit/pull/39) | His authorization and authenticated Requester regression | **Changes requested**, 2026-09-15. His `vitest.config.ts` excluded five suites, so `npm test` reported 32 passed while **29 tests were failing behind the exclusion** — and the PR cited that green summary as its evidence. Found by watching the total: a count that falls while files are added is the tell. Proved by overwriting the config with an include-only one and re-running. | Fixed in `96919a8`: the exclusions are gone, the suites were **authenticated rather than weakened**, 58 passed, and he added the CLOSED/CANCELLED attachment test I asked for. | **Approved** and merged as `beb3b2a`. A live probe confirmed his authorization surface: a forged `requesterId` refused in query and body, all three CSRF/Origin refusals, cross-owner `404` byte-identical to a nonexistent id, and logout revoking server-side |
| Earth2509 issue #40 / [Earth2509 PR #46](https://github.com/Earth2509/toktickit/pull/46) | His IT Staff Ticket Queue | **Changes requested**, 2026-09-15, on `f2879d1`. His summary said Vitest "could not start in this environment"; it ran here and reported **1 failed, 59 passed** — the failure being the test his own PR added. The cause was real: `validateStaffQueueQuery` evaluated `currentStatus`, `requestedPriority`, `itPriority` and the id filters *inside the returned object literal*, below an early `if (errors.length) return`, so `?currentStatus=unknown` answered `200` with the **unfiltered** queue. A combined query hid four of the five; only one bad parameter at a time exposed it. | Fixed in `0145fa3` by resolving every filter before the error check, with `it.each` coverage per filter that also asserts the query was never run. | **Approved**, 2026-09-15, and merged. I re-verified by calling the validator directly, one bad parameter at a time: all five plus `ownerId` rejected individually, and the combined `page=0 AND currentStatus=unknown` now reports **both** errors where it previously reported only `page` |
| Earth2509 issue #41 / [Earth2509 PR #47](https://github.com/Earth2509/toktickit/pull/47) | His ticket ownership, IT Priority and status workflow | **Changes requested**, 2026-09-16, on `753ad87`, from driving his real endpoints rather than reading the diff. Three blocking: reopening a terminal ticket did not clear an **ineligible historical owner**, so a RESOLVED ticket owned by a deactivated staff member returned to active work still owned by them — required in three places in his own contract, with no test; `409` was returned where his `api-spec.md` mandates `422` for invalid transition evidence, so "fix this field" reached the user as "reload"; and the concurrency coverage his issue and his own `tests.md` rows promised was absent, the "conflict" case asserting a mocked `updateMany` rather than a race. I led with proof that his *implementation* of the concurrency was correct — two simultaneous claims gave `[200,409]`, one event, one version increment — so the finding was about coverage, not correctness. | Fixed in `3cfc0fb`: reopen clears an ineligible owner inside the same transaction, the validator returns a discriminated `{ kind: "conflict" \| "validation" }` mapped to `409`/`422`, and he added an 8×8 transition test. He met the coverage point partly by narrowing the `tests.md` row rather than adding the concurrency test, which I recorded as a non-blocking follow-up. | **Approved**, 2026-09-16, and merged. Re-probed against real Postgres: short reason now `422`, reopen **clears** an inactive owner and **retains** an eligible one, and prohibited-transition and missing-owner correctly **stayed `409`** — the 422 split did not leak into the cases his contract keeps at 409 |
| Earth2509 issue #42 / [Earth2509 PR #48](https://github.com/Earth2509/toktickit/pull/48) | His ticket discussions: Public Comments, Internal Notes and the Requester's resolution indication | **Changes requested twice.** [Round one](https://github.com/Earth2509/toktickit/pull/48#pullrequestreview-5225621243), 2026-09-16, on `70cbb30`: every one of the five discussion rules behaved correctly when I probed his real endpoints, so the finding was coverage — 28 lines of pure-function unit test against 101 new lines of route, with his own `tests.md` naming a `comments-notes.api.test.ts` that did not exist. I named the exact one-line risk: adding `internalNotes` to `ticketDetailInclude` would ship every note to Requesters with his suite still green. [Round two](https://github.com/Earth2509/toktickit/pull/48#pullrequestreview-5226333995), on `4bf0a11`: he added the API suite and fixed five of six points properly, but I **applied that exact one-line break and his whole suite still passed** — 17 files, 84 passed, nothing failed. The structural cause was that every API suite in his repository mocks `../../src/prisma.js`, so no test anywhere exercises the real serialisation of a ticket. | He took the second option I offered and asserted the *query shape* — `expect(detailQuery?.include).not.toHaveProperty("internalNotes")` — which is the honest way to make that claim inside a mocked suite, with the reason written in a comment. He also added append-only coverage and **widened** the API-13 row to name both protections, restoring two items that had dropped out as `Planned`. | [**Approved**, 2026-09-17](https://github.com/Earth2509/toktickit/pull/48#pullrequestreview-5231967415) after re-applying my break and watching it fail this time; merged as `40c341d`. Non-blocking follow-up left with him: no API suite in his repository runs against a real database |
| Earth2509 issue [#43](https://github.com/Earth2509/toktickit/issues/43) / [Earth2509 PR #49](https://github.com/Earth2509/toktickit/pull/49) | His Administrator user management: the user list, search and role filter, creation, editing, activation, initial-password reset, and the Users workspace — 499 additions | [**Changes requested**, 2026-09-17](https://github.com/Earth2509/toktickit/pull/49#pullrequestreview-5238461923), on `cfb8342`. I led with proof that his **implementation is correct on every rule I could test**, from eight probes against his real endpoints on an isolated schema: role checked before lookup with a ghost id answering identically, two Administrators deactivating each other simultaneously leaving exactly one active, self-deactivation and last-Admin demotion both `409`, case-folded duplicate email `409`, reset revoking live sessions, and deactivation unassigning active-work tickets while a terminal ticket keeps its owner. The blocking finding is that the two mechanisms **his own api-spec names** — the `pg_advisory_xact_lock` that serialises last-Administrator changes and the in-transaction ticket unassignment — are untested: I removed each and his **entire** server suite stayed green, because his suite mocks `$transaction` and hands it `$executeRawUnsafe: vi.fn()`. Also blocking: no `UserManagement.test.tsx` exists at all, while his issue #43 requires non-Admins denied every route **and screen**. Non-blocking: `unassignActiveTickets` does `if (write.count !== 1) continue;`, so a concurrently-updated ticket keeps a deactivated owner with no event. | He closed both blocking findings in `7800e16` and `8ffcc08` — **tests and documentation only, with no source change**, so the behaviour I had already probed clean was untouched and only the coverage claim needed testing. He added a real-PostgreSQL integration suite on a dedicated disposable schema proving both mechanisms, and — the part that matters — **mocked assertions in the default suite as well**, so deleting either mechanism reddens a plain `npm test` rather than only an opt-in run. He also added the missing `UserManagement.test.tsx` and an authenticated-shell regression proving IT Staff are offered no Users control. | [**Approved**, 2026-09-17](https://github.com/Earth2509/toktickit/pull/49), on `8ffcc085`, after re-running both breaks against his **whole** suite rather than reading the new tests: removing the advisory lock now fails `users-admin.api.test.ts:106` and the integration race proof, and removing the unassignment fails `:116` and the transaction proof — both were green against the same breaks a round earlier. Breaking his client gate fails `AuthenticatedShell.test.tsx:99`. His `tests.md` rows are all honestly `Planned` rather than falsely `Passed`, which I said so in the review — the opposite of the promise-narrowing I had flagged three times earlier in the sprint |
| Earth2509 issue [#44](https://github.com/Earth2509/toktickit/issues/44) / [Earth2509 PR #50](https://github.com/Earth2509/toktickit/pull/50) | His Lab 3 E2E and responsive evidence: a new `role-workflows.spec.ts`, the Playwright config moved to `artifacts/lab-03`, and the evidence sections of his `tests.md` | [**Changes requested**, 2026-09-17](https://github.com/Earth2509/toktickit/pull/50#pullrequestreview-5241265936), on `6c16bc8`. His numbers reproduced here — 11 passed, 41.9s. The blocking finding was **an assertion that cannot fail**: his Administrator test ended by asserting the initial-password screen was absent, but his own `signIn` helper *completes* that gate before the assertion runs, so it was true whatever the server did. Proved by deleting the rule itself — `app.ts:629`, `mustChangePassword: true` to `false` — and re-running that one test: still **1 passed, exit 0**. Non-blocking: two documented evidence paths that nothing writes to, the third instance of that pattern. | He fixed it in one commit: `signIn(..., { expectMandatoryPasswordGate: true })` asserts the gate **before** the helper clears it, with the reason in a comment, and the unfailable line is gone. Both documented paths now match the filesystem, and he resolved "recorded with the tested commit" by versioning the command and its result while leaving generated files ignored — the right reading. | [**Approved**, 2026-09-18](https://github.com/Earth2509/toktickit/pull/50#pullrequestreview-5245662263) after re-applying the identical break and watching it **fail** this time, `1 failed, exit 1`, where it had passed a round earlier. I also ran the full suite rather than his focused rerun: 11 passed, 34.7s. Merged as `b144cb9`. **A positive control mattered here**: his `e2e:server` builds to `dist/` before serving, so an edit that never reached the running process would look identical to a rule that does not exist — relaxing his admin guard turned the same test red, which is what made the first green meaningful |
| Earth2509 issue [#45](https://github.com/Earth2509/toktickit/issues/45) / [Earth2509 PR #51](https://github.com/Earth2509/toktickit/pull/51) | His Lab 3 release: `lab3-staging` integrated into `main` — 73 files, +4845 −692, 32 commits | [**Approved**, 2026-09-18](https://github.com/Earth2509/toktickit/pull/51#pullrequestreview-5246612004). **Every command he recorded reproduced exactly here** — server 18 files passed and 2 skipped, 92 passed and 4 skipped; client 7 files and 24 tests; both builds; e2e 11 passed, 34.3s against his 38.3s. The two skipped files are his opt-in migration and admin-integration suites, not presented as passing. Also checked and clean: dropping `cors` follows from his same-origin proxy rather than being scope drift, and no secrets or tracked artifacts. | No changes were requested. One obligation the PR could not discharge, raised non-blocking: his issue #45 asks for evidence naming the tested **main** commit, but `Closes #45` fires on merge and no such commit exists beforehand — so I suggested a follow-up evidence-only PR on merged `main`, which is how our own #57 is written. | **Approved** and merged as `5b76198`. **A near-miss worth recording: I almost reported his eight `Passed` rows as premature, and they are correct by *his* convention** — his Final column records execution against the commit it ran on, while ours reserves `Passed` for `main`. Third time in this sprint that checking whose contract a rule belongs to stopped a false finding |
| Earth2509 PR [#52](https://github.com/Earth2509/toktickit/pull/52) | His README correction: one sentence, +1 −1, describing the repository as built through reviewed feature branches for Labs 1 to 3 | [**Changes requested**, 2026-09-18](https://github.com/Earth2509/toktickit/pull/52#pullrequestreview-5246849672). The wording was right and the size was right; **the base branch was the defect**. It targeted `lab3-staging` an hour after he merged staging into `main` in #51, so it could never reach the README a reader opens. Verified rather than assumed: `main`'s line 3 still carried the stale Lab 1 sentence, and `compare main...lab3-staging` returned `behind`, `ahead_by=0`, no differing files. Bundled in the same review: the post-merge `main` evidence had still not landed and #45 was already closed, so nothing tracked it. | He retargeted to `main`, rebased on the release commit, and **added a commit I had not asked for and had never seen** — `082d2e8`, which applies the three later Lab 3 migrations before the migration proof queries through the current Prisma client, whose `Ticket` model needs columns such as `itPriority` that the auth migration alone does not create. | [**Approved**, 2026-09-18](https://github.com/Earth2509/toktickit/pull/52#pullrequestreview-5250116764) and merged as `0381a8d`. I reviewed his added commit as new work rather than waving it through: his numbers reproduced, his `postAuthMigrationDirectories` is **complete** against the four migrations his repository actually has, and **removing the loop fails 1 of 2 tests while a no-op control stayed green at 2** — so the fix is load-bearing rather than merely harmless |
| Earth2509 PR [#53](https://github.com/Earth2509/toktickit/pull/53) | His final `main` verification evidence: a 127-line raw capture and a traceable command summary in `tests.md` | [**Approved**, 2026-09-19](https://github.com/Earth2509/toktickit/pull/53#pullrequestreview-5250831804). **All four recorded commands reproduced here** — server 18 files and 92 tests with 2 files and 4 tests skipped, migration 1 file and 2 tests, client 7 files and 24 tests, and e2e 11 passed at 39.1s against his 31.9s. `0381a8d` is genuinely #52's merge commit, confirmed by its parents `5b76198` and `082d2e8` rather than by its message. | No changes were requested. The capture retains the runner's own ANSI escapes and per-file timings, which is the difference between evidence and a description of evidence, and is what his own §7 asks for. He also resolved the regress I had raised on #51: recording against `0381a8d` from a documentation-only branch means the code state measured is exactly the code state that merges. | **Approved** and merged as `dbf453e`. Non-blocking left with him: the `=== Source ===` SHA header is the one **typed** line in an otherwise generated capture, so beginning it with the output of `git rev-parse HEAD` would make the file prove its own provenance |
| Earth2509 PR [#54](https://github.com/Earth2509/toktickit/pull/54) | Completing his Lab 3 responsive evidence: Login, Create Ticket, Requester Ticket Detail, User Management create and edit, and the mandatory Change Password gate, each at three breakpoints | [**Approved**, 2026-09-19](https://github.com/Earth2509/toktickit/pull/54#pullrequestreview-5255262667). His Verification section said the browser run had to be repeated locally because his sandbox cannot start Vite, so I ran it: **11 passed, 52.6s, exit 0**. That sentence has meant two different things this sprint — a real environment limit on his #49, and a concealed failure on his #46 — so the review says which this is rather than implying it. **I counted the captures instead of trusting the green run: nine screens at three viewports, 27, plus 3 for the gate, with no screen missing from any viewport.** The viewports in `role-workflows.spec.ts` are `1440x900`, `820x1180` and `390x844`, matching his `tests.md` exactly — worth checking because that array is not in the diff, so the documentation was making a claim about code the PR does not show. Every capture is gated by an overflow assertion *before* the screenshot, and no row moved off `Planned`. | No changes were requested. | **Approved**; the merge is his to make, and the merge commit is recorded at release. Non-blocking, left with him: there are now **three copies** of the same capture helper — `captureResponsiveEvidence` in lab-02, `capture` in role-workflows, and `capturePublicScreen` added by this PR — each re-implementing the 1px overflow threshold that *is* the responsive standard, so relaxing one leaves the other two green; and the Edit User capture is the Administrator editing **their own** account, the case his safety rules constrain, rather than an ordinary user. **Deliberately not re-raised:** the captures land in ignored output directories, which he and I settled on his #50 — the command and its output are the evidence. I also recorded a mistake of my own in the review: my first two attempts to count his captures returned **zero**, because I pointed at the wrong directory and did not account for Playwright hash-truncating test folder names. Trusting either would have told him this PR produces no evidence, which was flatly false — **a zero from a search is a statement about the search until it is shown to be a statement about the world** |

---

## Notes

- Rows are added as reviews happen, with the link to the review itself, not reconstructed at
  the end of the sprint.
- A review given as "Changes requested" is recorded as that, followed by the response and the
  eventual approval as separate facts. It is never rewritten as an approval.
